import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movespace.app',
  appName: 'MoveSpace',
  webDir: 'dist/MoveSpace/browser'
};

export default config;
